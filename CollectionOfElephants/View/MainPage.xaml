<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CollectionOfElephants"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:CollectionOfElephants.ViewModel"
    x:Class="CollectionOfElephants.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <ViewModel:MainViewModel/>
        </Grid.DataContext>

        <ListView HorizontalAlignment="Left" ItemsSource="{Binding Elephants, Mode=TwoWay}" SelectedItem="{Binding SelectedElephantModel, Mode=TwoWay}" Height="224" Margin="182,370,0,0" VerticalAlignment="Top" Width="100"/>

        <Image HorizontalAlignment="Left" Height="421" Margin="513,281,0,0" VerticalAlignment="Top" Width="628" Source="{Binding SelectedElephantModel.imageURL, Mode=TwoWay}"/>

        <TextBlock HorizontalAlignment="Left" Margin="554,247,0,0" TextWrapping="Wrap" Text="{Binding SelectedElephantModel.Name, Mode=TwoWay}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Left" Margin="909,247,0,0" TextWrapping="Wrap" Text="{Binding SelectedElephantModel.Weight}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Right" Margin="0,256,120,0" TextWrapping="Wrap" Text="New elephant name:" VerticalAlignment="Top" RenderTransformOrigin="0.756,0.615" Style="{StaticResource SubtitleTextBlockStyle}"/>
        <TextBox HorizontalAlignment="Left" Margin="1264,245,0,0" TextWrapping="Wrap" Text="{Binding NewElephaneModel.Name, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="1264,319,0,0" TextWrapping="Wrap" Text="{Binding NewElephaneModel.EarSize, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="1264,282,0,0" TextWrapping="Wrap" Text="{Binding NewElephaneModel.Weight, Mode=TwoWay}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Right" Margin="0,331,120,0" TextWrapping="Wrap" Text="Earsize" VerticalAlignment="Top" RenderTransformOrigin="0.756,0.615" Style="{StaticResource SubtitleTextBlockStyle}"/>
        <TextBlock HorizontalAlignment="Right" Margin="0,294,120,0" TextWrapping="Wrap" Text="Weight" VerticalAlignment="Top" RenderTransformOrigin="0.756,0.615" Style="{StaticResource SubtitleTextBlockStyle}"/>
        <Button Content="Add new elephant" HorizontalAlignment="Left" Margin="1160,365,0,0" VerticalAlignment="Top" Width="171" Command="{Binding AddNewElephant}"/>
        <Button Content="Remove elephant!" HorizontalAlignment="Left" Margin="191,613,0,0" VerticalAlignment="Top" Command="{Binding RemoveSelectedElephant}"/>




    </Grid>
</Page>
