<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CollectionOfElephants"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:CollectionOfElephants.ViewModel"
    x:Class="CollectionOfElephants.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <ViewModel:MainViewModel/>
        </Grid.DataContext>
        
        <ListView HorizontalAlignment="Left" ItemsSource="{Binding Elephants, Mode=TwoWay}" SelectedItem="{Binding SelectedElephantModel, Mode=TwoWay}" Height="100" Margin="182,494,0,0" VerticalAlignment="Top" Width="100"/>
        
        <Image HorizontalAlignment="Left" Height="421" Margin="513,281,0,0" VerticalAlignment="Top" Width="628" Source="{Binding SelectedElephantModel.imageURL, Mode=TwoWay}"/>
        
        <TextBlock HorizontalAlignment="Left" Margin="554,247,0,0" TextWrapping="Wrap" Text="{Binding SelectedElephantModel.Name, Mode=TwoWay}" VerticalAlignment="Top" Style="{StaticResource SubheaderTextBlockStyle}"/>




    </Grid>
</Page>
